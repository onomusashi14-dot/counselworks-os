# Browser always calls /api/* (same-origin). The Next.js server proxies
# that path to the upstream below. Default points at prod Railway — set
# API_PROXY_TARGET to override (e.g. a locally running API).
# API_PROXY_TARGET=http://localhost:4000
